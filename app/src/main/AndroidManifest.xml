<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.phstudio.freetv">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Storage permissions removed — using SAF (Storage Access Framework) instead -->

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FreeTV">

        <!-- Cast SDK options provider -->
        <meta-data
            android:name="com.google.android.gms.cast.framework.OPTIONS_PROVIDER_CLASS_NAME"
            android:value="com.phstudio.freetv.player.CastOptionsProvider" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".favorite.FavoriteActivity" />

        <activity android:name=".ui.AboutActivity" />
        <activity android:name=".ui.CategoriesActivity" />
        <activity android:name=".ui.LanguagesActivity" />
        <activity android:name=".ui.CountriesActivity" />
        <activity android:name=".ui.RegionsActivity" />

        <activity android:name=".player.PlayerActivity"
            android:configChanges="screenSize|smallestScreenSize|screenLayout|orientation|keyboardHidden"
            android:resizeableActivity="true"
            android:supportsPictureInPicture="true"
            android:exported="true"
            tools:ignore="UnusedAttribute" />
        <activity
            android:name=".player.HTMLActivity"
            android:configChanges="orientation|screenSize"
            android:hardwareAccelerated="true" />

        <activity android:name=".ui.LinkActivity" />
        <activity android:name=".custom.CustomActivity" />

    </application>
</manifest>
